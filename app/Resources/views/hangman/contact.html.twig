{% extends 'body.html.twig'  %}
{% block content %}
    <h2>Contact</h2>
    {{ form_start(formView) }}
    {{ form_errors(formView) }}
    {{ form_row(formView.surName) }}
    {{ form_row(formView.lastName) }}
    {{ form_end(formView) }}
{% endblock %}
